# 2026-03-23

## Pipeline Fixes
- **Zero-commits ping**: Added `<@U020AAXG1DE>` to the automated zero-commits warning in monitor.sh line 1765. Previously the `run_notify` call didn't include the ping — I had been posting manual follow-up pings which was inconsistent.
- **Context order issue**: Robert says the "order of input context" wasn't fixed as discussed. I have no memory of this discussion — asked him to clarify. Current fix-feedback.md order: Feedback → Task Description → Product Goal → User Request → Images → Plan → Implementation Changes.
- Both `logo-svg-data-uri` and `ref-images-text-glow` had fix agents flagged as zero commits today.
- `ref-images-text-glow` was a **false positive** — root cause: race condition in monitor.sh. Steps run in order: (1) check-agents.sh reports stale status='succeeded' from old phase, (2) gh-comment-dispatch sets phase='fixing' and spawns new agent, (3) main loop sees fixing+succeeded, does zero-commit check before agent has time to work. Fix: dispatch-fix.sh now sets `lastMonitorAction` timestamp to trigger the 60s race guard, skipping the task until next monitor cycle.
- `logo-svg-data-uri` was a real zero-commit — agent short-circuited with "Build already confirmed successful."
- **Context order discussion** (from thread on logo-svg-data-uri): Robert asked to put feedback FIRST in fix-feedback.md so agents can't skip it. This was done. The template now has: Feedback → Instructions → Context (task, goal, user request, plan, changes).
