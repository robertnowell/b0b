# 2025-03-01 — Pipeline Marathon

## Key Events
- Pipeline ran all night with 6 tasks in plan_review awaiting Robert's approval, no human input for 12+ hours
- **Massive token burn**: Monitor cron fired every 2 min, each time I emitted a full pipeline status post — dozens of identical "no changes this cycle" updates. Need to fix this: suppress status posts when nothing changed, or only post every N cycles.
- Approved and dispatched: `smart-respawn`, `consolidate-clawdbot`, `svg-preview-broken`, `svg-upload-no-alert`
- `consolidate-clawdbot` made it all the way to **merged** (PR created)
- `svg-preview-broken` and `svg-upload-no-alert` hit Codex usage limits during audit, but eventually completed through pipeline
- `smart-respawn` hit needs_split (max iterations, Codex limit) — still unresolved
- New tasks spawned: `pipeline-cron-comparison`, `fix-merged-phase` — both reached plan_review
- `workspace-pipeline-v2` replanned after rejecting `workspace-pipeline-support` (wrong architecture)

## Tasks in plan_review (awaiting Robert)
1. `pipeline-gaps-eval` — staleness alerts + smart respawn eval
2. `pipeline-task-timestamps` — per-task age in notifications
3. `ios-visibility-bug` — server-authoritative job tracking via Firebase RTDB
4. `pipeline-cron-comparison` — evaluate OpenClaw cron vs launchd for monitor
5. `workspace-pipeline-v2` — workspace task support in pipeline
6. `fix-merged-phase` — fix merged phase detection in monitor

## PRs in review
- `fix-arg-too-long`, `svg-preview-broken`, `svg-upload-no-alert`

## Failed tasks analysis (from Robert's request)
- 5 of 6 failed tasks are obsolete (targeted old `.clawdbot/` paths or superseded)
- `smart-respawn-v2` worth salvaging as manual workspace edit (~65 lines in monitor.sh)
- Robert hasn't confirmed cleanup yet

## Critical Lesson
**STOP SPAMMING STATUS UPDATES.** When nothing changes, either skip the post entirely or batch into much longer intervals. The current behavior burned enormous tokens posting identical status every 2 minutes for hours. This is the #1 thing to fix in the pipeline monitor integration.

## Codex Usage Limit
- Hit limit, resets Mar 5. Multiple tasks affected.
- Need fallback to Claude Code for auditing when Codex is unavailable.

## Consolidation: kopi-claw-config → Rendition .clawdbot/
- Copied all 21 plan files to `Rendition/.clawdbot/plans/`
- Removed pipeline/, plans/, prompts/, .clawdbot/ from kopi-claw-config (36 files)
- kopi-claw-config now only has workspace identity files (SOUL.md, AGENTS.md, etc.)
- All future plan links should use: `https://github.com/tryrendition/Rendition/blob/main/.clawdbot/plans/<name>.md`
- Robert's directive: everything including plans should be in Rendition repo
