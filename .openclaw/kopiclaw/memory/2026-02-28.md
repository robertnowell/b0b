
## Pipeline Status Formatting Rules (from Robert)
- Always use categorized format: Active → In Review → Merged → Failed
- Merged & Failed tasks >24h old: hide from status posts
- Active and In Review tasks: always shown regardless of age
- Within each section, sort oldest to newest by startedAt
- Include relative timestamps for each task (e.g. "2h ago")

## Cleanup — Workspace Pipeline Tasks Failed
- Marked failed: workspace-pipeline-support, smart-respawn, planning-in-monitor, planning-in-monitor-v2
- Removed 6+ worktrees (all workspace + old merged ones)
- Closed PR #1639 (workspace-pipeline-v2)
- Root cause: pipeline only creates worktrees from Rendition repo, can't handle workspace repo
- Decision: apply workspace changes manually to kopi-claw-config until multi-repo pipeline exists
- Still active: ios-visibility-bug (plan_review, ready for approval)

## Slack message tool
- Use `target` not `channel` for the channel ID parameter in `action=send`
- `channel` param doesn't work (gives "Unknown channel" error)
