# 2026-03-24

## K&L Email Pipeline — Suduiraut2 Status
- Suduiraut2 pipeline (clear-comet, pid 50975) was running with CustomEdit executing 4 ops
- Post-pipeline needs: (1) restore product table with 4 format SKUs from original HTML, (2) add pre-arrival disclaimer 9/15/2026 manually, (3) verify Ryan Moses review removed from wine block
- Pre-arrival date was omitted from pipeline inputs to avoid rubric recovery hang

## Pending Items
- Old Malt Rare Gems may need re-run when pull quote + reviews come in
- Chartron bottle shots pending from Murray — may need re-run
- Suduiraut2 post-pipeline fixes still needed (check pid 50975 status)

## Running Totals
- ~29 emails processed total (including re-runs)
- Known buyer IDs documented in summary

## Key Learnings (carried forward)
- CustomEdit remove_block and remove_review are unreliable — always use direct `edit` tool
- filter_reviews in CustomEdit/tweak also unreliable
- Single-SKU buyer review = intro rule (confirmed by Jo)
- Subtitle URL must be fixed manually post-pipeline when Robert provides custom search URL
- Pre-arrival disclaimers now pipeline-native after parseInputs regex fix (2026-03-14)
- Omit pre-arrival from pipeline inputs if CustomEdit removes wine blocks (causes rubric hang)
